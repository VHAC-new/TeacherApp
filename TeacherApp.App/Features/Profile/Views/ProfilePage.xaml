<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:TeacherApp.App.Features.Profile.ViewModels"
             x:Class="TeacherApp.App.Features.Profile.Views.ProfilePage"
             x:DataType="vm:ProfileViewModel"
             Title="Profile">

    <ScrollView BackgroundColor="{AppThemeBinding Light={StaticResource PageBackground}, Dark={StaticResource OffBlack}}">
        <VerticalStackLayout Spacing="0">

            <!-- Header gradiente -->
            <Border StrokeThickness="0" Padding="24,48,24,32"
                    StrokeShape="RoundRectangle 0,0,28,28">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="{StaticResource Primary}" Offset="0.0" />
                        <GradientStop Color="{StaticResource Tertiary}" Offset="1.0" />
                    </LinearGradientBrush>
                </Border.Background>
                <VerticalStackLayout Spacing="4">
                    <Label Text="My Profile" FontSize="30" FontAttributes="Bold" TextColor="White" />
                    <Label Text="Track your progress" FontSize="16" TextColor="#E6FFFFFF" />
                </VerticalStackLayout>
            </Border>

            <VerticalStackLayout Spacing="16" Padding="16,16,16,32">

                <!-- Cartão do perfil -->
                <Border StrokeThickness="0" Padding="20"
                        StrokeShape="RoundRectangle 24">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="{StaticResource Primary}" Offset="0.0" />
                            <GradientStop Color="{StaticResource Tertiary}" Offset="1.0" />
                        </LinearGradientBrush>
                    </Border.Background>
                    <Grid ColumnDefinitions="Auto,*" ColumnSpacing="16">
                        <Border WidthRequest="72" HeightRequest="72"
                                StrokeShape="RoundRectangle 36"
                                BackgroundColor="#33FFFFFF" Stroke="#4DFFFFFF" StrokeThickness="3">
                            <Image WidthRequest="36" HeightRequest="36"
                                   HorizontalOptions="Center" VerticalOptions="Center">
                                <Image.Source>
                                    <FontImageSource Glyph="&#xe7fd;"
                                                     FontFamily="MaterialIcons"
                                                     Color="White" Size="36" />
                                </Image.Source>
                            </Image>
                        </Border>
                        <VerticalStackLayout Grid.Column="1" Spacing="2" VerticalOptions="Center">
                            <Label Text="{Binding DisplayName}" FontSize="22" FontAttributes="Bold" TextColor="White" />
                            <Label Text="{Binding Email}" FontSize="14" TextColor="#CCFFFFFF" />
                        </VerticalStackLayout>
                    </Grid>
                </Border>

                <!-- Tema -->
                <Border Padding="16"
                        StrokeShape="RoundRectangle 16"
                        Stroke="{AppThemeBinding Light={StaticResource CardBorder}, Dark={StaticResource Gray600}}" StrokeThickness="1"
                        BackgroundColor="{AppThemeBinding Light=White, Dark={StaticResource OffBlack}}">
                    <Border.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding ToggleThemeCommand}" />
                    </Border.GestureRecognizers>
                    <Grid ColumnDefinitions="Auto,*" ColumnSpacing="16">
                        <Border WidthRequest="48" HeightRequest="48"
                                StrokeShape="RoundRectangle 14" StrokeThickness="0"
                                BackgroundColor="{StaticResource Secondary}">
                            <Label Text="{Binding ThemeEmoji}" FontSize="22" HorizontalOptions="Center" VerticalOptions="Center" />
                        </Border>
                        <VerticalStackLayout Grid.Column="1" Spacing="2" VerticalOptions="Center">
                            <Label Text="{Binding ThemeTitle}" FontSize="16" FontAttributes="Bold" />
                            <Label Text="{Binding ThemeSubtitle}" FontSize="14" TextColor="{StaticResource Gray500}" />
                        </VerticalStackLayout>
                    </Grid>
                </Border>

                <!-- Ações da conta -->
                <Border Padding="16"
                        StrokeShape="RoundRectangle 16"
                        Stroke="{AppThemeBinding Light={StaticResource CardBorder}, Dark={StaticResource Gray600}}" StrokeThickness="1"
                        BackgroundColor="{AppThemeBinding Light=White, Dark={StaticResource OffBlack}}">
                    <Grid ColumnDefinitions="Auto,*" ColumnSpacing="16">
                        <Border WidthRequest="48" HeightRequest="48"
                                StrokeShape="RoundRectangle 14" StrokeThickness="0"
                                BackgroundColor="#1A512BD4">
                            <Image WidthRequest="24" HeightRequest="24"
                                   HorizontalOptions="Center" VerticalOptions="Center">
                                <Image.Source>
                                    <FontImageSource Glyph="&#xe8b8;"
                                                     FontFamily="MaterialIcons"
                                                     Color="{StaticResource Primary}" Size="24" />
                                </Image.Source>
                            </Image>
                        </Border>
                        <VerticalStackLayout Grid.Column="1" Spacing="2" VerticalOptions="Center">
                            <Label Text="Settings" FontSize="16" FontAttributes="Bold" />
                            <Label Text="Manage your account" FontSize="14" TextColor="{StaticResource Gray500}" />
                        </VerticalStackLayout>
                    </Grid>
                </Border>

                <Border Padding="16"
                        StrokeShape="RoundRectangle 16"
                        Stroke="{AppThemeBinding Light={StaticResource CardBorder}, Dark={StaticResource Gray600}}" StrokeThickness="1"
                        BackgroundColor="{AppThemeBinding Light=White, Dark={StaticResource OffBlack}}">
                    <Border.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding LogoutCommand}" />
                    </Border.GestureRecognizers>
                    <Grid ColumnDefinitions="Auto,*" ColumnSpacing="16">
                        <Border WidthRequest="48" HeightRequest="48"
                                StrokeShape="RoundRectangle 14" StrokeThickness="0"
                                BackgroundColor="#1AEF4444">
                            <Image WidthRequest="24" HeightRequest="24"
                                   HorizontalOptions="Center" VerticalOptions="Center">
                                <Image.Source>
                                    <FontImageSource Glyph="&#xe879;"
                                                     FontFamily="MaterialIcons"
                                                     Color="#EF4444" Size="24" />
                                </Image.Source>
                            </Image>
                        </Border>
                        <VerticalStackLayout Grid.Column="1" Spacing="2" VerticalOptions="Center">
                            <Label Text="Sign Out" FontSize="16" FontAttributes="Bold" TextColor="#EF4444" />
                            <Label Text="Come back soon!" FontSize="14" TextColor="{StaticResource Gray500}" />
                        </VerticalStackLayout>
                    </Grid>
                </Border>

            </VerticalStackLayout>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
